<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.whut.bear.gather.dao.RecordDao">
    <!-- int saveRecord(Record record); -->
    <insert id="saveRecord">
        insert into t_record(id, user_id, student_number, real_name, class_number, class_name, health_upload_id,
                             schedule_upload_id,
                             closed_upload_id, upload_date, health_image_url, schedule_image_url, closed_image_url)
        VALUES (null, #{userId}, #{studentNumber}, #{realName}, #{classNumber}, #{className}, #{healthUploadId},
                #{scheduleUploadId},
                #{closedUploadId},
                #{uploadDate}, #{healthImageUrl}, #{scheduleImageUrl}, #{closedImageUrl})
    </insert>

    <!-- Record getUserRecordByDate(@Param("userId") Integer userId, @Param("date") Date date); -->
    <select id="getUserRecordByDate" resultType="Record">
        select *
        from t_record
        where user_id = #{userId}
          and upload_date = #{date,jdbcType=DATE}
    </select>

    <!-- int updateRecordState(Record record); -->
    <update id="updateRecordState">
        update t_record
        set health_upload_id   = #{healthUploadId},
            health_image_url   = #{healthImageUrl},
            schedule_upload_id = #{scheduleUploadId},
            schedule_image_url = #{scheduleImageUrl},
            closed_upload_id   = #{closedUploadId},
            closed_image_url   = #{closedImageUrl}
        where id = #{id}
    </update>

    <!-- List<Record> getLoginByNotUploaded(@Param("classNumber") Integer classNumber, @Param("date") Date date) -->
    <select id="getLoginByNotUploaded" resultType="Record">
        SELECT *
        FROM t_record
        WHERE class_number = #{classNumber}
          AND upload_date = #{date,jdbcType=DATE}
          AND (health_upload_id = -1 OR schedule_upload_id = -1 OR closed_upload_id = -1)
    </select>

    <!-- List<Record> getRecordUploaded(@Param("classNumber") Integer classNumber, @Param("date") Date date); -->
    <select id="getRecordUploaded" resultType="Record">
        SELECT *
        FROM t_record
        WHERE class_number = #{classNumber}
          AND upload_date = #{date,jdbcType=DATE}
          AND health_upload_id != -1
          AND schedule_upload_id != -1
          AND closed_upload_id != -1
    </select>
</mapper>

