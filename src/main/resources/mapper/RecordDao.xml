<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.whut.bear.gather.dao.RecordDao">
    <!-- int saveRecord(Record record); -->
    <insert id="saveRecord">
        insert into t_record(user_id, student_number, real_name, class_number, class_name, status, health_upload_id,
                             schedule_upload_id,
                             closed_upload_id, upload_date, health_image_url, schedule_image_url, closed_image_url)
        VALUES (#{userId}, #{studentNumber}, #{realName}, #{classNumber}, #{className}, #{status}, #{healthUploadId},
                #{scheduleUploadId}, #{closedUploadId}, #{uploadDate}, #{healthImageUrl}, #{scheduleImageUrl},
                #{closedImageUrl})
    </insert>

    <!-- Record getUserRecordByDate(@Param("userId") Integer userId, @Param("date") Date date); -->
    <select id="getUserRecordByDate" resultType="Record">
        select *
        from t_record
        where user_id = #{userId}
          and upload_date = #{date,jdbcType=DATE}
    </select>

    <!-- int updateRecordState(Record record); -->
    <update id="updateRecordState">
        update t_record
        set status             = #{status},
            health_upload_id   = #{healthUploadId},
            health_image_url   = #{healthImageUrl},
            schedule_upload_id = #{scheduleUploadId},
            schedule_image_url = #{scheduleImageUrl},
            closed_upload_id   = #{closedUploadId},
            closed_image_url   = #{closedImageUrl}
        where id = #{id}
    </update>

    <!-- List<Record> getClassRecordList(@Param("classNumber") Integer classNumber, @Param("date") Date date, @Param("status") Integer status); -->
    <select id="getClassRecordList" resultType="Record">
        select *
        from t_record
        where status = #{status}
          and upload_date = #{date,jdbcType=DATE}
          and class_number = #{classNumber}
    </select>

    <!-- List<Record> getGradeRecordList(@Param("grade") Integer grade, @Param("date") Date date, @Param("status") Integer status); -->
    <select id="getGradeRecordList" resultType="Record">
        select *
        from t_record
        where status = #{status}
          and upload_date = #{date,jdbcType=DATE}
          and grade = #{grade}
        order by class_number
    </select>
</mapper>

