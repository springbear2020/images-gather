<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.whut.bear.gather.dao.UploadDao">
    <!-- int saveUpload(Upload upload); -->
    <insert id="saveUpload">
        insert into t_upload(user_id, file_type, upload_time, bucket, domain, `key`)
        VALUES (#{userId}, #{fileType}, #{uploadTime}, #{bucket}, #{domain}, #{key})
    </insert>
</mapper>

