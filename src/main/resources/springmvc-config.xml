<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">
    <!-- Controller 组件扫描 -->
    <context:component-scan base-package="edu.whut.bear.gather.controller,
                                          edu.whut.bear.gather.interceptor,
                                          edu.whut.bear.gather.exception"/>
    <!-- 默认 Servlet 处理静态资源 -->
    <mvc:default-servlet-handler/>
    <!-- MVC 注解驱动以支持高级功能 -->
    <mvc:annotation-driven/>

    <!-- Thymeleaf 视图解析器 -->
    <bean id="viewResolver" class="org.thymeleaf.spring5.view.ThymeleafViewResolver">
        <property name="order" value="1"/>
        <property name="characterEncoding" value="UTF-8"/>
        <property name="templateEngine">
            <bean class="org.thymeleaf.spring5.SpringTemplateEngine">
                <property name="templateResolver">
                    <bean class="org.thymeleaf.spring5.templateresolver.SpringResourceTemplateResolver">
                        <property name="prefix" value="/WEB-INF/templates/"/>
                        <property name="suffix" value=".html"/>
                        <property name="templateMode" value="HTML5"/>
                        <property name="characterEncoding" value="UTF-8"/>
                    </bean>
                </property>
            </bean>
        </property>
    </bean>

    <!-- 拦截器 -->
    <mvc:interceptors>
        <!-- 用户登录检查 -->
        <mvc:interceptor>
            <!-- 拦截除主页、登录、注销、静态资源外的所有请求 -->
            <mvc:mapping path="/**"/>
            <mvc:exclude-mapping path="/"/>
            <mvc:exclude-mapping path="/login"/>
            <mvc:exclude-mapping path="/logout"/>
            <mvc:exclude-mapping path="/static/**"/>
            <ref bean="loginInterceptor"/>
        </mvc:interceptor>
        <!-- 用户权限检查 -->
        <mvc:interceptor>
            <!-- 拦截用户资源 -->
            <mvc:mapping path="/user/**"/>
            <ref bean="userInterceptor"/>
        </mvc:interceptor>
        <!-- 管理员权限检查 -->
        <mvc:interceptor>
            <!-- 拦截管理员资源 -->
            <mvc:mapping path="/admin/**"/>
            <ref bean="adminInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 异常处理器 -->
    <bean  class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
        <property name="exceptionMappings">
            <props>
                <!-- 当遇到自定义的拦截器异常时，跳转到首页登录页面 -->
                <prop key="edu.whut.bear.gather.exception.InterceptorException">index</prop>
            </props>
        </property>
        <!-- 为异常信息设置一个属性名，值为 loginErrorMsg，默认在 request 域中进行共享 -->
        <property name="exceptionAttribute" value="loginErrorMsg"/>
    </bean>

    <!-- 视图控制器 -->
    <mvc:view-controller path="/" view-name="index"/>
    <mvc:view-controller path="/user/home" view-name="user/home"/>
    <mvc:view-controller path="/admin/home" view-name="admin/home"/>
    <mvc:view-controller path="/admin/record" view-name="admin/record"/>
</beans>